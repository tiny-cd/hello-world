# hello-world
Hello world repo for tiny-cd for testing

## Usage

Build the Docker image:
```bash
docker build -t hello-world .
```

Run the container:
```bash
docker run hello-world
```

This will output "hello world" to the terminal and exit.
